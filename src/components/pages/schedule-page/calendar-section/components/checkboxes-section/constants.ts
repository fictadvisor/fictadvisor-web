import { Checkboxes } from '@/store/schedule/useSchedule';

export const initialValues: Checkboxes = {
  addLecture: true,
  otherEvents: false,
  addPractice: true,
  addLaboratory: true,
  isSelective: false,
};
